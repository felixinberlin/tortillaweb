---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title?: string;
  description?: string;
  lang?: string;
  showHeader?: boolean;
  showFooter?: boolean;
}

const props = Astro.props;
---

<BaseLayout {...props}>
  <slot />
</BaseLayout>
